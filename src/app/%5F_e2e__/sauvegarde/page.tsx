import SaveHarnessPage from "../../__e2e__/sauvegarde/page";

// Next réserve les segments commençant par `_`. `%5F` expose littéralement le premier
// underscore de `/__e2e__` tout en gardant le harnais source dans le dossier demandé.
export const dynamic = "force-dynamic";

export default SaveHarnessPage;
